# US-AD-9 admin rate-limit zones.
#
# Include this file once in nginx `http {}` context (e.g. /etc/nginx/nginx.conf).
limit_req_zone $binary_remote_addr zone=admin_console_per_ip:10m rate=60r/m;
limit_req_zone $binary_remote_addr zone=admin_login:10m rate=10r/m;
